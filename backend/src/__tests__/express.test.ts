describe("Express API tests", () => {
    it("check auth", async () => {
        // const gotFromDb = await GetFromDb("123", "google")
        // expect(gotFromDb).not.toBe(false)
        // const wrongFromDb = await GetFromDb("123", "spotify")
        // expect(wrongFromDb).toBe(false)
    });
    it("report something", async () => {
        // const reportSomething = await ReportSomething("123", "google", "12345", null, "something")
        // expect(reportSomething).toBe(true)
        // const deleteReport = await DeleteReport("123", "google", "12345")
        // expect(deleteReport).toBe(true)
    });
});
